### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  user: User!
}

"""Available currencies according to ISO 4217"""
enum Currency {
  AUD
  CNY
  EUR
  GBP
  JPY
  USD
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Mutation {
  addPayment(amount: Float!, currency: Currency!, description: String, name: String!): Payment
  addPaypal(accountName: String, username: String!): Paypal
  addSepa(accountName: String, iban: String!): Sepa
  deletePayment(id: Int!): Payment
  deletePaypal(id: Int!): Paypal
  deleteSepa(id: Int!): Sepa
  login(password: String!, username: String!): AuthPayload!
  markPaymentPaid(id: Int!): Payment
  signup(email: String!, firstName: String!, lastName: String!, password: String!, username: String!): AuthPayload!
  updatePayment(amount: String, currency: Currency!, description: String, id: Int!, name: String): Paypal
  updatePaypal(accountName: String, id: Int!, username: String): Paypal
  updateSepa(accountName: String, bankName: String, bic: String, iban: String, id: Int!): Sepa
}

type Payment {
  amount: Float!
  createdAt: DateTime!
  createdBy: User!
  currency: String!
  description: String
  id: ID!
  isPaid: Boolean!
  name: String!
}

type Paypal {
  accountName: String!
  id: Int!
  user: User!
  username: String!
}

type Query {
  payment(id: Int): Payment
  user(onlyActive: Boolean, username: String): User
  users: [User]!
}

type Sepa {
  accountName: String
  bankName: String!
  bic: String!
  iban: String!
  id: Int!
  user: User!
}

type User {
  avatarUrl: String
  createdAt: DateTime!
  email: String!
  firstName: String!
  id: ID!
  lastName: String!
  lastSignin: DateTime!
  payments: [Payment]!
  paypal: [Paypal]!
  sepa: [Sepa]!
  username: String!
}
